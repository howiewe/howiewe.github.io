# AGENTS.md

給 AI 助手的專案協作準則與工程約定。

---

## 協助開發時遵守

請務必照著下面的順序走，不要跳過：

### 1. 先搞懂再動手
- **先看主題地圖**：專案架構劃分請見 [`docs/README.md`](docs/README.md) 的地圖。新增功能或重構前，先確認屬於哪個主題、放在對應位置，不破壞現有分工。
- **動手前先讀通相關程式碼**：使用者要求改動時，先把前後端相關程式碼完整閱讀過再規劃，不只依賴文件文字。文件只記「形狀」，真實細節都在 code 裡，以 code 為準。
- **跨層問題先建框架再診斷**：若改動涉及資料庫 (D1)、API 路由、SSR 注入 (HTMLRewriter) 或前端狀態等多個層級，先讀主題地圖建立整體框架，再把**整條相關呼叫鏈**一次讀通，避免因局部視野導致反覆推翻。

### 2. 開發與修改原則
- **低耦合、職責單一**：新模組盡量拆成獨立小函式/類別，只透過參數注入，不依賴其他模組的私有內部結構。
- **不硬編碼 (No Hardcoding)**：常數、API 路由前綴、設定值集中管理。SQL 查詢**嚴格禁止拼接字串**，全面使用 D1 Prepared Statement (`.prepare().bind()`)。
- **UI 保持簡潔好操作**：維持既有設計語言與 CSS 變數系統，深淺色模式切換透過 `var(--...)` 保持統一，不寫死特定顏色 有手機電腦響應式設計。
- **零外部 CDN 依賴**：新增第三方 library 一律下載至 `vendor/` 本地引用，不依賴外部公共 CDN。

### 3. 註解規範
- **只寫「為什麼」，不寫廢話**：註解用來解釋業務背景與「為什麼這樣設計」；逐行解說「這行在做什麼」或 `💡 新增`、`// 修正` 等標記為雜訊，不要添加。

### 4. 順手清理技術債
- 套用精簡原則：改動相關功能時，順手清理不再使用的舊實作、重複邏輯與過時註解，保持代碼庫輕盈整潔。

---

## 安全邊界（嚴格禁止）

1. **不可隨意變更 Functions 路由結構**：`/admin*`、`/api/*`、`/public/*` 已與 Cloudflare Access 安全策略綁定，不可擅改路徑前綴。
2. **不可破壞資料庫向下相容性**：不任意刪除既有 D1 欄位；`imageUrls` 欄位格式必須維持 `[{"url":"...","size":90}]` 物件陣列 JSON 字串。
3. **不可在 Functions 代碼內自造 Token 驗證**：後台與寫入型 API 完全依賴 Cloudflare Access 邊緣防護，避免雙重認證衝突。

---

## 維護文件 (docs/) 的時機

**改程式的過程中不要動 docs/，改完、使用者確認後才更新。**

- **改程式過程中** → 專注於將功能與代碼改對，不分心改動文件。
- **功能改完、使用者確認後** → 才依「實際改動結果」更新 `docs/`。
- **以程式碼為主**：當文件與程式碼有出入時，**以 code 為準**。

---

## 兩種情況，兩種對待

- **開頭自動讀取時** → 記住規則，保持專注，不自動觸發文件更新。
- **當使用者在對話裡 `@AGENTS.md` 引用時** → 這是明確訊號：**「這次改動定案了，現在同步現況」**。主動執行下方「同步現況流程」。

---

## 同步現況的流程（被 @ 時執行）

1. **盤點改動**：對照「判斷要不要動文件」，篩出真正改變系統架構、職責或資料流的部分。
2. **檢查 Code**：逐條對照「協助開發時遵守」（低耦合、不硬編碼、SQL 安全、註解品質），發現小瑕疵順手修整。
3. **更新 docs/**：修改對應主題文件的現況描述；未改動到的主題保持原樣。
4. **順手精簡**：去除重複、囉嗦或已被涵蓋的內容，維持文件「只記形狀不記尺寸」的精準度。
5. **同步地圖**：若有新增、刪除或重命名主題文件，同步更新 `docs/README.md` 的地圖連結。
