npx wrangler d1 execute product-catalog --remote --command="CREATE TABLE categories (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, parentId INTEGER, createdAt TEXT NOT NULL, updatedAt TEXT NOT NULL, FOREIGN KEY (parentId) REFERENCES categories(id));"
npx wrangler d1 execute product-catalog --remote --command="CREATE TABLE products (id INTEGER PRIMARY KEY AUTOINCREMENT, sku TEXT UNIQUE, name TEXT NOT NULL, ean13 TEXT, price REAL DEFAULT 0, description TEXT, imageUrls TEXT, imageSize INTEGER DEFAULT 90, categoryId INTEGER, createdAt TEXT NOT NULL, updatedAt TEXT NOT NULL, FOREIGN KEY (categoryId) REFERENCES categories(id));"